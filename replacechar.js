//split string into arrray
// find the string argument if x === char
//remove the index that i select
//join the array back together
// return the new string

function removeChar(string, char) {
  return string.replace(char, '');
  //   var split = string.split('');
  //   var rep = split
  //   for (let index = 0; index < split.length; index++) {
  //     var letter = split[index];
  //     if (letter === char) {
  //       var newArray = split.(char, index);
  //       console.log(newArray);
  //     }
  //   }
}

// console.log('hello');

removeChar('DeAndre', 'e');
